{% extends 'base.html.twig' %}

{% block title %}Créer une specialité{% endblock %}

{% block body %}

<h1>Créer une specialité</h1>

{{ form_start(form) }}
        {{ form_row(form.name) }}
        <div class="my-3">
            <button type="submit" class="btn btn-sm btn-warning">AJOUTER</button>
        </div>
        {{ form_end(form) }}

{% endblock %}