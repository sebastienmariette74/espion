{% extends 'base.html.twig' %}

{% block title %}Types de planque{% endblock %}

{% block body %}

<h1 class="mb-3">
    Types de planque
    <a href="{{ path('admin_typeHidingPlace_create') }}"><img src="{{ asset('build/images/plus.png') }}"
            alt="Ajouter un type de planque" class="img-fluid"></a>
</h1>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Nom</th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody class="table-group-divider">
        {% for type in typesHidingPlace %}
        <tr>
            <td>{{ type.name }}</td>
            <td><a href="{{ path('admin_typeHidingPlace_update' ,{id : type.id}) }}">
                    <img src="{{ asset('build/images/edit.png') }}" alt="" class="img-fluid">
                </a>
            </td>
            <td>
                <a href="{{ path('admin_typeHidingPlace_delete', {id : type.id}) }}">
                    <img src="{{ asset('build/images/delete.png') }}" alt="" class="img-fluid">
                </a>
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}