<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_admin') }}">Société d'Espionnage</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <img src="{{ asset('build/images/user-white.png') }}" alt="" class="img-fluid">
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="ms-auto">
        {% if app.user %}
        <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Se déconnecter</a>
      {% else %}
        <a class="nav-link" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
      {% endif %}
      </div>
    </div>
  </div>
</nav>